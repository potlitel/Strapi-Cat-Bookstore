'use strict';

/**
 * caracteristica-fisica service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::caracteristica-fisica.caracteristica-fisica');
